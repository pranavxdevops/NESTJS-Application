# Field-level validation messages
user.username.required=Username is required
user.email.invalid=Email must be valid
member.memberId.invalid=Member ID must be a UUID

# Generic fallback
validation.generic=Validation failed

# Constraint-level defaults
validation.isUuid=Value must be a valid UUID
validation.isString=Value must be a string
validation.isEmail=Value must be a valid email

# Field + constraint (overrides)
validation.memberId.isUuid=memberId must be a valid UUID
validation.companyName.isString=companyName must be a string
